# Production Environment Configuration

PORT=5000
# Connection string for your MongoDB instance.
# If installed locally on the VPS: mongodb://127.0.0.1:27017/gymguide
# If using a managed service (Atlas/Vultr Managed): provided by the dashboard
DATABASE_URL="mongodb://127.0.0.1:27017/gymguide"

# Secret key for signing JWT tokens.
# Generate a new one using: openssl rand -base64 32
JWT_SECRET="e939281a8c9b2e3f4d5e6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d"

# Optimizes the build for performance.
NODE_ENV="production"
